buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:0.8'
    }
}

apply plugin: 'shadow'

jar {

    manifest {
        attributes 'Implementation-Title': 'Hex Republics - Game Server',
                'Implementation-Version': version,
                'Built-By': System.getProperty('user.name'),
                'Built-Date': new Date(),
                'Built-JDK': System.getProperty('java.version'),
                'Main-Class': 'com.cogitareforma.hexrepublics.gameserver.GameServer'
    }
}

dependencies {
        compile project(':common')
}